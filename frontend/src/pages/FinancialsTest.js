import Layout from '@/components/Layout';

const FinancialsTest = ({ user, onLogout }) => {
  return (
    <Layout user={user} onLogout={onLogout}>
      <div>
        <h1>Test</h1>
      </div>
    </Layout>
  );
};

export default FinancialsTest;